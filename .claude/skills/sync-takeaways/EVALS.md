# Evaluations — sync-takeaways v2

Run `scripts/validate-skill.ps1`, then have a fresh evaluator apply `fixtures/candidates.json` without editing the vault.

1. Given an evidence register from `fill-daily-log`, history scans = 0 and linked notes are not reopened wholesale.
2. Missing reuse situation kills a summary; an empty result is valid; output has 0–3 bullets.
3. Procedure routes to candidate Skill; durable decision principle routes to candidate Constant.
4. Proposal stays `待拍板`; it is never phrased as a decided pattern.
5. SCQA material is a plain `SCQA candidate`, never `#scqa-feed`, until a live consumer/query exists.
6. Every shipped line has claim + concrete reuse situation + existing source + route; only the target Daily Note section changes.

PASS requires 6/6, no second history scan after fill, no dead routing tag, and no Critical/Important findings.
