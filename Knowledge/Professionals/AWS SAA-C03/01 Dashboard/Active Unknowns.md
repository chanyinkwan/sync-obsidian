
最多只能有三個。

1. How do temporary credentials sign an AWS API request and resolve to an IAM Role principal?
2. When should Explicit Deny be used instead of relying only on a narrower Allow?
3. How does an end-user identity relate to a backend workload identity across different architectures?

## Parking Lot
- [ ] 