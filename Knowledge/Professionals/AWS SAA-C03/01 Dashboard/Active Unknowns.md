
最多只能有三個。

1. What is the AWS policy evaluation order (Explicit Deny, then Allow, then Implicit Deny), and can it be applied correctly to a fresh scenario without mis-sequencing it?
2. How does an end-user identity relate to a backend workload identity across different architectures?
3. What are the three components of a temporary credential, and specifically what does the session token carry (Role identity and expiry, not request content)?

## Parking Lot
- [ ] 