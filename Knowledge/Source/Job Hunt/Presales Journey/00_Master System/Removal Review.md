# Removal Review

Reviewed: 2026-08-21

## Decision required

No item in this review has been removed. The candidates below need explicit user approval before any deletion. Empty `CV/`, `Research/`, and `Interview/` directories inside company-role records are **not** candidates: they are required record scaffolding for active or prospective roles.

| Current path | Why it is a candidate | Replacement / retained system location | Reversible through Git? |
|---|---|---|---|
| `Presales Journey/$On List/` | Empty legacy dashboard container after its two notes moved to Pipeline source records. | `Presales Journey/01_Pipeline/Application Pipeline.md`; historical notes at `01_Pipeline/Source Notes/`. | No — the empty directory itself is not tracked. |
| `Presales Journey/$Interview System/` | Legacy shared-system container now holds only stale Python bytecode under `__pycache__/`; all source materials moved to the Master System. | `Presales Journey/00_Master System/`. | No for the untracked bytecode/empty directory; source materials remain recoverable from Git history. |
| `Presales Journey/00_Master System/Tools/__pycache__/` | Generated bytecode duplicates `render_cv.py` and `test_render_cv.py`; it is not a source artefact. | `00_Master System/Tools/render_cv.py` and `test_render_cv.py`. | No — generated cache is untracked. |
| `Presales Journey/ai-native-physical-industries/` | Empty duplicate category directory; the retained category material is under `$Categories`. | `Presales Journey/$Categories/ai-native-physical-industries/Research - Block B Market.md`. | No — the empty directory itself is not tracked. |
| `Presales Journey/01_Pipeline/Source Notes/Inbox (legacy).md` | Legacy dashboard source retained only as reconstruction evidence. | `Presales Journey/01_Pipeline/Application Pipeline.md` and the company-role records. | Yes — its original tracked source is recoverable from Git history. |
| `Presales Journey/01_Pipeline/Source Notes/On List Jobs (legacy).md` | Legacy dashboard source retained only as reconstruction evidence. | `Presales Journey/01_Pipeline/Application Pipeline.md` and the company-role records. | Yes — its original tracked source is recoverable from Git history. |

Do not remove either Source Note until the user confirms that its historical reconstruction value is no longer needed.

